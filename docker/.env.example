# MongoDB container credentials
# Used by mongo-init.js to create the app user and database.
MONGO_INITDB_ROOT_USERNAME=caferoot
MONGO_INITDB_ROOT_PASSWORD=caferootpass

# App user — this is what your backend connects as
MONGO_APP_USERNAME=cafeapp
MONGO_APP_PASSWORD=cafeapppass
MONGO_APP_DATABASE=cafe_db